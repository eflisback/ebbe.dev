import styles from "./ProjectBox.module.css";

interface IProps {
  project: Project;
}

export default function ProjectBox({ project }: IProps) {
  return (
    <div>
      <span>test</span>
    </div>
  );
}
